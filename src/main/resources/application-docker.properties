# Active only inside the container (SPRING_PROFILES_ACTIVE=docker is set in the Dockerfile).
# Browser requests reach the container through Docker's network, so H2 sees them as
# remote. Allow them here; the port is only published on the host's 127.0.0.1.
spring.h2.console.settings.web-allow-others=true
